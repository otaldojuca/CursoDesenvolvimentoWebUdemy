// Arquivo package.json

// descreve todas as dependências que a aplicação precisa

// Digitar no terminal "cd /[pasta em que esse arquivo está]"
// Digitar então "npm init" ou "npm init -y" (mais rápido)

// Digitar "npm i --save axios"
